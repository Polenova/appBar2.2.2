<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.android.polenova">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SendingMailActivity"></activity>
        <activity android:name=".RandomImageActivity" />
        <activity android:name=".UserHealthActivity" />
        <activity android:name=".UserPressActivity" />
        <activity android:name=".InfoUserHealthActivity" />
        <activity android:name=".MoneyTransferActivity" />
        <activity android:name=".AddressActivity" />
        <activity android:name=".NotesActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>